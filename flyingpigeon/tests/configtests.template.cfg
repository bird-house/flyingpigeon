[subsetwfs]
wps_host:

[averagerwfs]
wps_host:
# Leave test_default_geoserver empty to skip (must be empty if no wps_host)
test_default_geoserver:

[pairing_fx_testdata_grid5]
pfx5_opendap:
pfx5_fileserver:

[testpoly01]
testpoly01_typename:
testpoly01_featureids:
testpoly01_geoserver:
